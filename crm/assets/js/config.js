/* ============================================================================
   Better Tap CRM — configuration
   Paste your two Supabase values below (from Supabase → Project Settings → API).
   The anon/public key is SAFE to ship in front-end code (it's public by design).
   NEVER paste the service_role key here.
   ============================================================================ */
window.BT_CONFIG = {
  SUPABASE_URL:      'PASTE_YOUR_PROJECT_URL',   // e.g. https://abcdxyz.supabase.co
  SUPABASE_ANON_KEY: 'PASTE_YOUR_ANON_KEY'       // the long "eyJ..." anon / public key
};
